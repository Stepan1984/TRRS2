apiVersion: v1
kind: Service
metadata:
  name: matrix7-service
  labels:
    app: matrix7
spec:
  selector:
    app: matrix7
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
